DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=
PORT=

# Módulo /crypto (Integrante 3) — llave maestra AES-256 (32 bytes en hex = 64 caracteres).
# Generarla localmente con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
HSM_MASTER_KEY=
